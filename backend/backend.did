type ShoppingItem = 
 record {
   category: text;
   completed: bool;
   id: nat;
   "text": text;
 };
type Category = 
 record {
   items: vec text;
   name: text;
 };
service : {
  addItem: (text, text) -> (nat);
  deleteItem: (nat) -> (bool);
  getCategories: () -> (vec Category) query;
  getItems: () -> (vec ShoppingItem) query;
  toggleItem: (nat) -> (bool);
}
