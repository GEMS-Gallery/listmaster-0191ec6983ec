type ShoppingItem = 
 record {
   category: text;
   completed: bool;
   icon: text;
   id: nat;
   "text": text;
 };
type Category = 
 record {
   icon: text;
   items: vec text;
   name: text;
 };
service : {
  addItem: (text, text) -> (nat);
  deleteItem: (nat) -> (bool);
  getCategories: () -> (vec Category) query;
  getItems: () -> (vec ShoppingItem) query;
  toggleItem: (nat) -> (bool);
}
